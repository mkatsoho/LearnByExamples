#! /usr/bin/python3

print( "# install activestate python3 as an none-root user, e.g. as a common user, install to the user's own directoryi - ./install.sh ")
print( "# create a soft link python3 (32/64bits) as root, e.g. /usr/bin/python3 -> /home/username/python/bin/python3, and each .py file use '#! /usr/bin/python3' at the 1st line ")
print( "# install cherrypy package to the installed python's home, e.g. python3 setup.py install, then cherrypy is installed to the python home of the used python3")

